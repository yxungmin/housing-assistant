# 앱은 anon 키로 공개 공고 데이터만 읽는다. 비어 있으면 번들된 data/announcements.json 으로 동작한다.
# 값을 채우면 Expo가 빌드 시 인라인한다 (EXPO_PUBLIC_ 접두사만 앱에 들어간다). service role 키는 절대 넣지 않는다.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
