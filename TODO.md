# 할 일

2026-09-21 기준. 완료된 것은 지우지 말고 체크만 한다. 결정이 필요한 항목은 **결정 필요**로 표시한다.
설계 문서: https://claude.ai/code/artifact/54a25418-ac2c-403c-9c5d-0169acb231fc

---

## 지금 진행 중

- [x] **모델 벤치마크 → 추출 모델 확정: claude-opus-5**
      대표 공고 5건(002·005·008·012·018)에 Opus 5 / Sonnet 5 / Haiku 4.5를 돌렸다.
      기준이 Opus 초안이라 정확도가 아니라 일치도다. Opus 재실행 89.0%가 잡음 바닥이고
      Sonnet 44.9%, Haiku 18.6%로 바닥과 격차가 크다. 가격은 89.7% / 10.3% / 0%.
      Sonnet과 Haiku는 가장 무거운 공고(SH 장기전세)에서 아예 실패했다.
      결과: `benchmark/output/report.*.json`, 다시 보려면 `npm run benchmark:compare`
- [x] **모델 단가 실측 확인** (2026-09-21) — 저장소 값이 맞았다. $15/$75가 아니다.
      9/20 청구액 **$21.62**. 같은 날 토큰(Opus 1.27M/0.50M · Sonnet 0.29M/0.14M · Haiku 0.51M/0.10M)을
      `MODEL_PRICES`로 계산하면 $22.58, $15/$75로 계산하면 $67.74다. 4% 차이는 캐시 토큰 배수 가정 탓.
      `MODEL_PRICES`는 고칠 것이 없고 확인 근거만 주석으로 남겼다.

## 신뢰 (최우선)

정보의 확실성이 이 서비스의 생명. 아래는 지금 신뢰를 깎고 있는 것들.

- [x] **검수하지 않은 추출을 VERIFIED로 표시하는 문제** — 상태를 셋으로 나눴다.
      `VERIFIED` 사람이 공고문과 대조함 (fixtures에서 온 것만) / `AUTO` 자동 추출·검증만 / `UNVERIFIED` 아직 조건을 못 읽음.
      `app-data.ts`가 붙이던 `verified: true`를 없앴다 — 지금 번들 11건은 전부 AUTO이고 verified=true인 룰은 0건이다.
      초안을 "조건 분석 중"으로 숨기지는 않았다. 그러면 자격이 되는 사람에게 공고를 감추게 되고 그 오류는 아무도 신고하지 않는다.
      대신 상세에서 사실대로 알린다: "자동으로 옮긴 조건이에요. 사람이 아직 확인하지 않았어요."
      자동 검증에서 걸린 것(012·014 "가격 정보 없음")은 그 줄에 그대로 띄운다.
      엔진도 고쳤다 — `verified`로 판정을 막지 않는다. 검수 여부는 화면이 알리고 판정은 있는 값으로 한다.
      - [x] **자동 게시** (`supabase/migrations/0006_auto_publish.sql`) — 자동 검증을 통과하면 `auto_publish_version()`이
            사람 승인 없이 게시한다. `publish_version()`은 사람 확인 도장만 찍고, 앱은 게시된 버전을 AUTO/VERIFIED로 구분해 받는다.
            자동 검증 지적을 둘로 나눴다: 숫자를 믿을 수 없는 것(`conflict_reasons`)은 게시 보류, 나머지(`checks`, 예: 가격 정보 없음)는
            게시하되 앱에 그대로 알린다. **아직 어느 DB에도 적용하지 않았다 — Supabase 프로젝트가 없다.**
- [x] **원문 공고문 PDF 링크를 앱에서 열 수 있게** — `pdf_url`을 앱 타입·원격 뷰·번들 데이터까지 연결했다.
      근거 시트에 "공고문 원문 23쪽 열기", 상세에 "공고문 원문 보기"(전체). 여는 것은 `src/lib/source.ts`.
      수집기는 기관 첨부 주소를 `benchmark/pdfs/meta.json`에 남긴다(`ResolvedNotice.pdf.url`).
      - [ ] **번들 데이터에는 018만 주소가 있다** — 나머지 10건은 `npm run benchmark:fetch`로 다시 받아야 채워진다
            (001~004는 MOCK이라 출처 URL 자체가 없다). 주소가 없으면 버튼을 내보이지 않는다.
      - 쪽 이동은 보장되지 않는다: Storage 주소(application/pdf)는 크롬에서 `#page`가 먹지만
        iOS 기본 뷰어는 무시하고, SH 첨부는 `Content-Disposition: attachment`라 내려받아진다. 화면에 한 줄로 알린다.
- [x] **"이 숫자 이상해요" 신고 동작 연결** — 항목 단위로 받는다.
      조건 한 줄·임대조건을 누르면 근거 원문이 펼쳐지고 거기서 신고한다(`ReportSheet`).
      신고는 기기에 먼저 쌓이고(`src/lib/reports.ts`) Supabase가 붙으면 자동으로 올라간다 —
      그래서 오프라인·미설정에서도 버튼이 동작한다. 신고한 줄에는 "확인 중"이 붙고 내 정보에 내역이 쌓인다.
      `issue_reports`에 대상·상태·처리 결과 컬럼을 더했다(`supabase/migrations/0005_issue_reports.sql`).
      - [x] **운영자가 상태를 바꾸는 화면** — 검수 뷰어(`npm run review`)에 신고 큐. 확인 중/처리함으로 나눠 보여 주고
            네 가지로 끝낸다(공고문과 같음·수정함·공고 정정·신고 아님). 적은 한 줄이 앱 신고 내역에 그대로 보인다.
            값을 실제로 고치는 것은 재추출·버전 게시가 한다 — 큐는 결과만 남긴다.
      - [ ] **수정 반영 알림** — 관심 공고의 값이 바뀌면 알려 준다 (`src/lib/notifications.ts` 사용).
      - [ ] 누락 신고는 받을 수 없다 — 사용자는 빠진 조건을 볼 수 없으니 신고할 수도 없다. 기계·사람 몫.
- [ ] **정정공고 이력 표시** — 무엇이 언제 바뀌었는지 배지로. 수집한 공고 중 실제로 정정공고가 있었다.
- [ ] **공고별 확인 시각 표시** — 지금은 내 정보에만 있다.
- [ ] **낮은 confidence 규칙은 "확인 필요"로 내리기** — 추정과 확정이 같은 무게로 보이면 안 된다.
- [ ] **계산 가정 명시 강화** — 관리비 추정, 대출 한도(LTV), 상환 방식 가정을 한 줄로.

## 무료 → 유료 전환

원가 구조상 매칭·계산은 기기에서 돌아 무료 사용의 한계비용이 0. 관문 위치를 바꾸는 게 핵심.

- [ ] **관문을 숫자 앞이 아니라 뒤로**
      보증금·월임대료는 공고문의 공개 사실이니 항상 무료. 잠글 것은 우리가 계산한 값
      (필요 현금, 부족액, 대출 비교, 시나리오). 잠긴 자리에 가려진 숫자와 설명을 둔다.
- [ ] **"평생 1건"을 "월 N건"으로** — 매달 초기화되는 무료 건수. 비용은 늘지 않고 재방문 이유가 생긴다.
- [ ] **시트를 닫아도 화면에서 내보내지 않기** — `app/announcement/[id]/cost.tsx`의 `router.back()` 제거.
- [ ] **요구 시점을 의도가 드러난 순간으로** — 관심 공고 2건, 시나리오 두 번째 조정, 마감 알림 켤 때.
- [ ] **가격을 사용자 자신의 숫자로 설명** — "보증금 조합만 바꿔도 월 N만 원 차이". 기기에서 이미 계산한 값.
- [ ] **체험의 전제를 숨기지 않기** — 스토어 7일 체험은 결제수단 등록이 필요하다.
      결제 없는 무료 건수를 앞에 두고 체험은 결제 화면의 혜택으로만 표기.
- [ ] **체험 종료 2일 전 알림** — 환불·분쟁을 줄이고 스토어 정책에도 맞다.

## 데이터 수집

- [x] LH 수집기 (공공데이터포털 분양임대공고 API)
- [x] SH 수집기 (공개 API 없어 게시판 HTML 파싱)
- [x] 공급기관 어댑터 `collector/src/sources.ts`
- [x] 수집 범위 제어 `COLLECT_PROVIDERS` / `COLLECT_REGIONS` (기본 LH·SH / 서울·경기)
- [ ] **SH 고유 유형 매핑 결정** — **결정 필요**
      희망하우징(공공기숙사), 두레주택, 사회주택, 장기안심주택, 도시형생활주택, 수요자맞춤형이
      지금 전부 `other`로 떨어진다. 화면에 "기타"로 나온다.
      `collector/src/lh/mapping.ts` 표에 어떻게 넣을지 정한다.
- [ ] **SH 마감일 없음 대응** — SH는 목록·상세에 접수 마감일이 없고 공고문 추출 결과로만 채워진다.
      추출 전에는 앱의 D-day가 비어 있다.
- [ ] **수집 실패 감시** — SH는 스크래핑이라 사이트 개편 시 깨진다. 실패하면 바로 알 수 있게.
- [ ] **마이홈포털 통합 API 활용신청** — https://www.data.go.kr/data/15108420/openapi.do
      자동승인·무료. SH는 포함하지 않지만 지방 도시공사(부산·대전·충남 등)를 한 번에 가져온다.
      지방 확장 시점에.
- [ ] GH(경기주택도시공사) 경로 확인 — 마이홈 표본에 없었다. SH처럼 별도 수집이 필요할 가능성.
- [ ] 같은 PDF면 재추출하지 않도록 내용 해시 비교 — 정정공고 한 건이 추출비 그대로 나간다.
- [ ] 푸시 구독 지역에 수요가 있는 곳부터 추출 (집계값이라 개인정보 아님)

## Supabase 연결

- [x] **Supabase 프로젝트 생성 + 마이그레이션 0001~0006 적용** (2026-09-21).
      마이그레이션은 빈 Postgres에 처음부터 적용해 동작까지 확인하고(`npm run db:check`) 올렸다.
      확인 중 `0004_provider.sql`이 빈 DB에서 실패하던 것을 고쳤다 — `create or replace view`는 컬럼을 중간에 못 끼운다.
- [x] 앱 `.env`에 `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` — 앱이 뷰를 읽고 신고를 올린다.
      테스트 공고 1건으로 신고 왕복(앱 → 서버 → 검수 큐 → FIXED + 처리 문구 → 앱)까지 확인했다.
- [ ] **`0007_nearby.sql` 적용** — 아직 아무 DB에도 안 올렸다. 순서: Docker 켜고 `npm run db:check`로
      빈 DB 전체 재적용을 통과시킨 뒤 Supabase SQL Editor에 붙여 넣는다. `app_announcements` 뷰를 다시 만든다.
- [ ] `supabase/seed/loan_products.sql` 적용
- [ ] `npm run collect` 실제 실행 → 공고·버전·PDF 저장 확인 (추출이 꺼져 있어 지금은 목록까지만 돈다)
- [ ] 앱 피드 증분 동기화 (`updated_at` 기준) + 지역 필터 — 지금은 매번 전체를 받는다

## 남은 일 (2026-09-21 작업에서 못 한 것)

이날 배우자 직장·위치/교통/주변 인프라까지 붙였다. 아래는 그 자리에서 못 끝낸 것들이고,
대부분 "키가 없다" 또는 "새 파이프라인이 필요하다"로 막혀 있다.

- [ ] **Kakao REST 키** — **막힘: 사용자 작업 필요**. 무료. `collector/.env`에 `KAKAO_REST_API_KEY`.
      없으면 좌표·지하철역·버스정류장·주변 인프라가 전부 빈 채로 저장되고 앱은 그 섹션을 통째로 숨긴다.
      지금 번들에서 교통·인프라가 보이는 3건(망원·봉천·과천)은 손으로 채운 값이다. 나머지 9건은 비어 있다.
- [ ] **실제 통근 시간** — **결정 필요**. 지금 화면이 말할 수 있는 건 직선거리뿐이다.
      "버스 몇 정거장 · 환승 1회 · 총 43분"을 말하려면 교통 API가 필요하다:
      ODsay(무료 티어 있음) / 카카오모빌리티 / 네이버 Directions 중 하나를 고르고,
      호출을 수집 때 1회로 묶을지(공고 × 직장 조합이라 사용자마다 달라진다) 앱에서 할지 정해야 한다.
      정하기 전까지 화면은 "실제 통근 시간은 아직 계산하지 않아요"라고 그대로 적는다 — 총 몇 분을 지어내지 않는다.
- [ ] **평면도** — 공고문 PDF 안의 평면도 이미지를 뽑아 Storage에 올리고 상세에서 보여 준다.
      LLM은 필요 없다(이미지 추출이라 과금 없음). 새로 만들어야 하는 것: 어느 이미지가 평면도인지 고르는 규칙,
      업로드 경로와 버전 관리, 앱의 이미지 뷰어.
- [ ] **앱 안 지도** — 지금은 좌표를 시스템 지도 앱으로 넘긴다(`lib/commute.ts`).
      카드 안에 지도를 그리려면 `react-native-maps` 추가 + 네이티브 재빌드(Android는 Google Maps 키도 필요).
- [ ] **`npm run db:check` 재실행** — Docker가 꺼져 있어 0007을 빈 DB에 적용해 보지 못했다.
      뷰 컬럼 검사에 `nearby`를 추가해 뒀으니 켜고 한 번 돌리면 된다.
- [ ] **iOS 시뮬레이터 런타임 설치** — 이 맥에 런타임이 하나도 없다(`xcrun simctl list runtimes`가 비어 있음).
      그래서 실행 순서(스플래시가 두 번 보이는 문제 같은 것)를 눈으로 확인하지 못하고 코드로만 판단했다.
      Xcode에서 iOS 런타임 하나 받아 두면 다음부터는 직접 눌러 보고 확인할 수 있다.

## 출력 스키마 줄이기 (비용) — **결정 필요**

실측: Opus 비용의 **70%가 출력 토큰**이다(9/20 입력 $6.1 / 출력 $12.6). 공고문이 길어서가 아니라
추출 결과 JSON이 길어서 비싸다. 공고 1건 출력이 약 28,000자 ≈ 23,800토큰.
어디가 큰지는 `npm run output:size`가 잰다 (`collector/src/output-size.ts`).

| 후보 | 출력의 | 성격 |
| --- | ---: | --- |
| `source.text` 근거 원문 | 15.2% | **건드리지 않는다.** 신고 시트가 보여 주고 `fixture:check`가 대조하는 근거다 |
| `applies_to`가 전부 null (240/530룰) | 9.6% | 룰마다 키 5개를 다 쓰게 돼 있다. 객체째 nullable이면 `null` 한 줄 |
| `payment_schedule` | 7.4% | 분양 전용인데 엔진은 rental만 받는다. **아무도 읽지 않는다** |
| `pricing`의 null 필드 | 6.2% | rental인데 `sale_price:null`, sale인데 `deposit:null`을 매 행 쓴다 |
| `notes` 5번째 이후 | 4.6% | 앱은 4개만 보여 준다 |
| `confidence` | 2.9% | 530개 중 523개가 0.8 이상이다. 변별력이 없다 |

근거 원문을 뺀 나머지를 다 걷어내면 **31% 절감**(월 약 13,000원).

**돈이 이유가 되기엔 작다.** 월 13,000원이고, 손대는 건 이 서비스의 전부인 추출 정확도다.
할 만한 이유는 따로 있다 — **출력이 길수록 틀릴 자리가 늘어난다.** 아무도 읽지 않는
`payment_schedule`과 240번의 `applies_to: 전부 null`은 모델이 만들어 낼 필요가 없는 값이다.

순서 (위험이 낮은 것부터):
- [x] `payment_schedule` 제거 — 분양 비용 계산을 붙이는 V0.2에 다시 넣는다. 내부 스키마와 DB 컬럼은 뒀다.
- [x] `notes` 개수 상한을 설명에 적는다 — "가장 중요한 것 6개까지". 구조화 출력이 min/max를 안 받아 문법이 아니라 설명으로.
- [x] `applies_to`를 객체째 nullable — `conversion`이 이미 같은 방식이라 문법 위험이 낮다.
      셋을 합쳐 **출력 16% 감소**(기존 11건 기준 309,581자 → 260,196자). `EXTRACTION_PROMPT_VERSION` v3 → v4.
      `notes` 상한은 모델이 지켜야 효과가 나므로 옛 데이터에는 안 잡힌다 — 실제로는 조금 더 줄어든다.
- [ ] **v4 벤치마크 1회** — **막힘: 사용자 작업 필요**. 아직 아무도 v4로 추출해 본 적이 없다.
      필요한 것: 공고문 PDF(`benchmark/pdfs/`가 비어 있다), `ANTHROPIC_API_KEY`, `EXTRACTION_ENABLED=true`.
      ```
      # collector/.env 에 ANTHROPIC_API_KEY, LH_API_KEY, EXTRACTION_ENABLED=true
      npm run benchmark:fetch -- --count 5     # PDF 받기
      npm run benchmark                        # 약 $4
      npm run benchmark:compare                # v3 결과와 일치도 비교
      ```
      끝나면 `EXTRACTION_ENABLED`를 다시 지운다. 일치도가 89%(Opus 자기 일치도) 근처면 회귀 없음으로 본다.
- [ ] `confidence`는 낮을 때만 (2.9%) — 다만 "낮은 confidence 규칙에 확인 필요 표시" 계획이 사라진다.
      쓸 생각이면 그대로 두고, 안 쓸 거면 빼는 게 맞다. **먼저 정할 것.**
- [ ] `pricing`을 kind별로 가르기 (6.2%) — 유니온은 구조화 출력 문법에서 가장 위험하다. 마지막에.

**어느 것도 검증 없이 넣으면 안 된다.** 스키마를 고치면 `EXTRACTION_PROMPT_VERSION`을 올리고
벤치마크를 다시 돌려야 하는데, 그건 추출을 켜는 일이라 돈이 나간다(5건 × 1회 ≈ $4).
게다가 Opus는 자기 자신과도 89%만 일치해서, 작은 정확도 하락은 그 잡음에 묻힌다 —
정답 fixture가 생기기 전에는 "나빠지지 않았다"를 증명하기 어렵다.

## 경쟁 앱 분석 — 지도형 공고 앱 (2026-09-21)

LH·SH·GH·HUG 공고를 지도로 보여 주는 앱 하나를 화면 녹화로 살펴봤다(113초, 전 탭).
우리와 정면으로 겹치는 부분이 있고, 우리가 안 하기로 한 것도 분명해졌다.

### 배울 것

- **지도 우선** — 전국 지도에 시도별 공고 수(서울 19, 경기 16 …)를 얹고 눌러 들어간다.
  "내 동네에 뭐가 있나"가 첫 화면에서 끝난다. 우리는 목록뿐이라 지역감이 없다.
- **원문 이미지 4종** — 지역조감도 · 단지조감도 · 단지배치도 · 동호배치도를 썸네일로 주고 전체화면으로 연다.
  주택형 표에 평면도 열도 있다. 사람들이 실제로 궁금해하는 것이고, 우리 TODO의 "평면도"보다 범위가 넓다.
- **원문으로 가는 길이 끊기지 않는다** — 스크롤 어디에서나 "공고문보기" 버튼이 떠 있고,
  맨 아래에 청약센터 / 공고문 웹페이지 두 버튼이 따로 있다.
- **면책을 분명히 말한다** — 상세 위쪽에 "신청 전 꼭 확인하세요 · 표시된 내용은 공고문을 이해하기 위한 요약입니다.
  자격·일정·제출서류는 공고문 원문을 기준으로 확인해 주세요", 맨 아래에 "이 앱은 편의 도구이며 법적 효력이 없습니다".
  우리가 `SourceCard`로 넣은 방향과 같다.
- **필터에 건수를 같이 보여 준다** — LH 102건 / SH 3건 / GH 1건, 국민임대 47건 / 매입임대 25건 …
  누르기 전에 결과 크기를 안다. 우리 칩은 건수가 없다.
- **미열람 표시** — 안 본 공고에 빨간 "미열람". 새 공고를 놓치지 않게 한다.
- **커버리지** — 전국 · 기관 6곳(LH/SH/GH/HUG/기타) · 123건. 우리는 서울·경기 LH·SH.
- **검색** (공고명·단지명·지역), **접수일정 탭**.

### 우리가 안 할 것 (그 앱의 약점)

- **자격 판정이 없다.** "내 알림"은 지역·공고 유형·공급 대상만 맞춘다.
  소득·자산·무주택 기간으로 "나는 되나?"를 답하지 않는다. **이게 우리가 있는 이유다.**
- **공고문을 그대로 쏟아붓는다.** 1~4순위 표가 법령 문장 그대로 들어가 있어, 다 읽어도 내 얘긴지 모른다.
- **숫자 버그가 그대로 노출된다** — 전용면적이 "37.29999999999999997(m²)". 부동소수점을 안 다듬었다.
  숫자를 파는 앱에서 이런 게 보이면 나머지 숫자도 의심받는다.
- **전면 비디오 광고 + 배너 광고.** 마감이 걸린 정보를 보는 중에 광고가 끼어든다.
  우리는 구독 모델이라 안 넣어도 된다 — 그 자체가 차별점이다.
- **인앱 브라우저가 느리다** — apply.lh.or.kr이 8초 넘게 빈 화면이었다.
- **비용 계산이 없다.** 보증금·월세를 보여 줄 뿐 "내가 얼마를 마련해야 하나"는 없다.
- **정확도 표시가 없다** — 어디까지가 자동이고 사람이 봤는지 알 수 없다. 우리는 VERIFIED/AUTO/UNVERIFIED로 나눈다.

### 반영 계획

우리 강점(자격 판정 · 비용 계산 · 출처 표시)을 지키면서 격차가 큰 것부터.

- [ ] **필터 칩에 건수** — 가장 싸고 효과가 크다. `matchAll` 결과를 한 번 더 세면 된다.
- [ ] **안 본 공고 표시** — `changes.ts`에 이미 `seen` 개념이 있다. 저장한 공고의 변경뿐 아니라
      "처음 뜬 공고"에도 같은 표시를 붙인다.
- [ ] **원문 이미지(조감도·배치도·평면도)** — TODO의 "평면도" 항목을 여기로 합친다.
      먼저 확인할 것: LH API가 이미지를 주는가, 아니면 공고문 PDF에서 뽑아야 하는가.
- [ ] **지도 뷰** — 다만 전국 지도가 아니라 **"내 조건에 맞는 공고"만 얹은 지도**로 한다.
      전국 공고를 다 찍으면 그 앱과 같아지고, 우리가 거른 결과라는 점이 사라진다.
      `react-native-maps` 추가 + 네이티브 재빌드가 필요하다.
- [ ] **검색** (공고명·지역) — 목록이 길어지면 필요해진다. 지금 12건에서는 아니다.
- [ ] **접수일정 뷰** — 마감 기준 달력. 지금은 마감 3일 전 알림만 있다.
- [ ] **기관 확대 (GH·HUG)** — `sources.ts`에 `Source` 하나씩. 파이프라인은 기관과 무관하다.

하지 않기로 한 것: 광고, 공고문 원문 통째 렌더링, 전국 커버리지 우선 확대
(정확도를 검증할 수 있는 범위를 먼저 굳힌다).

## 보안 (급함)

- [ ] **자격증명 3건 교체** — 2026-09-21 터미널에 노출됐다. 아직 유효하면 그대로 살아 있다.
      - Supabase secret key (`sb_secret_…`) — 대시보드 Settings → API에서 rotate
      - GitHub PAT (`ghp_…`)
      - Atlassian API token

## 검수

- [ ] **정답 fixture 만들기** — 초안 11건을 검토해 `benchmark/fixtures/`로 옮긴다.
      이게 있어야 벤치마크가 일치도가 아니라 정확도를 잰다.
      - [x] 1차 대조 도구 `npm run fixture:check` — 룰·가격마다 인용문이 그 쪽에 있는지, 값이 인용문과 맞는지 본다.
            지적을 쪽 단위로 묶어 주고, 사람이 확인한 뒤 `--promote --reviewed-by <이름>`으로 승격한다.
      - [x] 018(SH 장기전세) 대조 — 243건 중 기계 통과 58, 확인 필요 185(전부 표 재구성 인용), 문제 0. 검수 단위 14개.
      - [ ] **018 사람 확정** — `npm run fixture:check -- 018` 목록을 공고문과 대조하고 승격.
      - [ ] **001~014 PDF 재확보** — **막힘**. `benchmark/pdfs/`는 git 제외라 클론에 없다.
            005·007·008·010·012·014는 LH PAN_ID를 알고 있어 `LH_API_KEY`만 있으면 받는다.
            001~004는 `MOCK-` id에 출처 URL 기록이 없어 원본 파일이 있어야 한다.
- [ ] 검수 뷰어(`npm run review`, 4310)에 추출 버튼 — 사람이 고르고 기계가 돌린다
- [ ] 목표치 대비 판정: 룰 정확도 95%, 공고 완전일치 80%, 가격 98%, 누락 3% 이하, 없는 조건 생성 1% 이하

## 앱

- [x] 온보딩 → 홈 목록 진입, 무료 계산 카드
- [x] 직장 위치(시군구 좌표), 직장 20km 필터
- [x] **배우자 직장 위치** — 신혼·예비신혼부부에게 한 번 더 묻는다(`workplace_partner`).
      "직장 근처" 필터와 정렬은 두 사람 중 **더 먼 쪽**(`commuteKm`)을 쓴다 — 한 사람만 가까운 집은 후보가 아니다.
- [x] **위치·교통 상세** — 지하철/버스를 갈라 보여 주고(호선 포함), 도보는 "약 N분 (Nm)".
      주변 인프라(어린이집·학교·마트·편의점·병원·공원)는 종류별 최근접 1곳. 지도는 시스템 지도 앱으로 넘긴다.
      전부 직선거리라고 화면에 그대로 적는다.
      → 여기서 못 끝낸 것(실제 통근 시간·Kakao 키·평면도·앱 안 지도)은 위의 "남은 일 (2026-09-21)"에 모아 뒀다.
- [x] 건보료 소득 도우미
- [x] 구독 상태 머신 + 결제 어댑터(로컬 목)
- [x] 알림(마감 3일 전 기기 예약, 푸시 토큰 등록)
- [x] 무주택 기간 년+개월 입력
- [x] 시트 딤 고정, 헤더·하단 버튼 sticky, 어절 단위 줄바꿈, 하트 애니메이션
- [ ] 공급기관 배지 (LH / SH) — 0004 마이그레이션이 뷰에 내려 준다
- [ ] "현재 LH·SH 서울·경기만 제공" 고지 — 목록 숫자가 전부인 것처럼 보이면 안 된다
- [ ] 면적 필터 — 경쟁 앱 리뷰에서 가장 많이 나온 요청
- [ ] 알림 유형 필터 (관심 없는 유형이 오지 않게)
- [ ] 분양(sale) 비용 계산은 V0.2. 지금 엔진은 rental만 받는다

## 출시 준비

- [ ] 스토어 결제 구현 (RevenueCat 또는 react-native-iap) — 지금은 `src/lib/billing.ts`의 로컬 목
- [ ] 영수증 검증 Edge Function → `subscriptions` 테이블이 진실 원본
- [ ] 신규 공고 푸시 발송 Edge Function (M7)
- [ ] 통근 시간 API (Edge Function 프록시)
- [ ] Google Play 개발자 등록 ($25 일회성)
- [ ] Apple Developer Program ($99/년) — iOS를 낼 경우
- [ ] 개인정보처리방침·이용약관 — 프로필은 기기에만 저장한다는 점 명시
- [ ] 면책 문구 — 공공기관 공식 앱이 아님, 참고용, 접수 전 원본 공고문 확인

---

## 비용 차단 (테스트 기간)

**2026-09-21부터 추출을 꺼 뒀다.** 돈이 나가는 곳은 LLM 추출 한 군데뿐이다.

- `EXTRACTION_ENABLED` 기본값 `false` — `collector/src/llm/extract.ts`가 호출 전에 거부한다. 수집기·벤치마크·`inspect --extract` 전부 막힌다.
- 수집기는 이 상태에서 목록·신규 판정까지만 하고 쓰기도 하지 않는다.
- `.github/workflows/collect.yml`의 30분 cron을 주석 처리했다 (`workflow_dispatch`는 남겨 둠).
- 다시 켤 때: `.env`에 `EXTRACTION_ENABLED=true`, Actions는 변수 `EXTRACTION_ENABLED=true` + cron 주석 해제.

## 운영비 기준선

서울·경기, LH + SH, 임대만. 실측 공고 수 기준.

| 항목 | 값 |
| --- | --- |
| LH 서울·경기 임대 | 월 19.7건 |
| SH 임대 모집공고 | 월 7.7건 |
| 정정 재추출 여유 | 15% |
| 구독 1건 실수령 | 2,241원 (2,900원에서 부가세·스토어 15% 제외) |

### 월 비용 (2026-09-21 기준)

**지금 실제로 나가는 돈은 개발자 계정뿐이다 (약 14,500원/월).** 추출이 꺼져 있어 LLM 비용은 0이다.
아래는 추출을 켜고 정식 운영할 때.

| 항목 | 월 | 근거 |
| --- | ---: | --- |
| LLM 추출 | 41,600원 | 31.5건 × 1,319원 (claude-opus-5) |
| Supabase | 0원 | Free 플랜 (DB 500MB / Storage 1GB) |
| GitHub Actions | 0원 | 무료 한도 내 |
| Kakao Local | 0원 | 무료 (일 10만 호출) |
| Expo 푸시 | 0원 | 무료 |
| Apple Developer ($99/년) | 11,600원 | |
| Google Play ($25 일회성) | 2,900원 | 첫 해만 월할. 둘째 해부터 0 |
| **합계** | **약 56,000원** | 둘째 해부터 약 53,000원 |

**손익분기 26명** (실수령 2,241원 기준). 둘째 해부터 24명.
환율은 1,400원/$로 잡았다.

### 단가는 확인됐다 (2026-09-21)

한때 $5/$25냐 $15/$75냐로 3배 불확실성이 있었다. **실제 청구액으로 $5/$25가 맞다고 확인했다.**

9/20 하루치 청구액 $21.62. 같은 날 토큰을 `MODEL_PRICES`로 계산하면 $22.58($15/$75였다면 $67.74).

거기서 1건당 추출비를 역산하면 **약 1,194원**이다 — Opus 몫 약 $17.9를 그날 돌린 추출 21회
(벤치마크 5건 × 2회 + 초안 11건)로 나눈 값. 위 표는 1,319원으로 잡아 뒀는데,
초안 11건이라는 수가 정확하지 않을 수 있어 **보수적인 쪽을 그대로 둔다**.
실측값을 쓰면 월 37,600원 · 합계 52,100원 · 손익분기 24명이 된다.

### 위 표에 아직 안 들어간 것

이 표는 **지금 만들어 둔 것만**의 비용이다. 남은 작업을 붙이면 늘어난다.

- **사람 검수 인건비** — 0원으로 잡혀 있지만 실제로는 가장 큰 비용이다. Opus가 자기 자신과도 89%만 일치하니
  검수를 건너뛸 수 없고, 매달 공고 31건을 누군가 공고문과 대조해야 한다.
- **교통 API** — ODsay 무료 티어는 일 1,000회 수준이다. 공고 × 사용자 직장 조합으로 쓰면 금방 넘는다.
- **Supabase Free 한도** — 공고 PDF를 Storage에 쌓으면 1GB는 몇 달이면 찬다. 넘으면 Pro $25/월(약 35,000원).
- **GitHub Actions** — 30분 cron을 다시 켜면 월 1,440회다. private 저장소 무료 한도(2,000분)의 70%를 쓴다.

**Opus가 자기 자신과도 89%밖에 일치하지 않는다.** 같은 공고를 두 번 돌리면 조건의 11%가 달라진다는 뜻이다.
사람 검수를 건너뛸 수 없다는 근거이고, 정확도 목표 95%는 정답 fixture가 생긴 뒤에 다시 판단한다.
